apiVersion: v1
kind: Service
metadata:
  name: webui
  namespace: squawk
  labels:
    app.kubernetes.io/name: squawk
    app.kubernetes.io/component: frontend
    app.kubernetes.io/instance: webui
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app.kubernetes.io/name: squawk
    app.kubernetes.io/component: frontend
    app.kubernetes.io/instance: webui
